# DeepSeek Trading Bot - Session Notes & Architecture Reference

This document serves as a comprehensive reference of the trading bot's architecture, features, and improvements made up to the current session. Use this file as a reference for future modifications.

## 1. Core Architecture Overview

The system is a fully automated algorithmic intraday trading bot running on the **DhanHQ API**. It evaluates trades on a primary 3-minute timeframe using a combination of hard technical pre-filters, market regime analysis, and an LLM (DeepSeek) as the final decision engine. 

### Key Components:
- **`main.py`**: The entry point. Initializes logging, loads environment variables, configures the `IntradayStockBot` and `PositionExitGuardian`, and runs them concurrently using `asyncio.gather`.
- **`stock_trading_bot.py` (`IntradayStockBot`)**: The primary scanner and entry engine. Scans the watchlist every 3 minutes.
- **`position_exit_guardian.py` (`PositionExitGuardian`)**: A standalone fast-polling daemon (30s) dedicated entirely to monitoring open positions and executing intelligent, graduated exits.
- **`dhan_integration.py` (`DhanStockTradingBot`)**: Handles all Dhan API interactions, rate-limit backoffs, caching (historical & live data), and Super Order placement/cancellations.
- **`deepseek_analyzer.py`**: The AI module that reads technical indicators and market context to return a `BUY/SELL/HOLD` signal with confidence scores.
- **`regime_filter.py`**: Evaluates Nifty 50 and Sectoral index daily data to determine the broader market trend (bullish/bearish/neutral) and volatility.
- **`reversal_detector.py`**: A purely mathematical script evaluating 8 distinct technical reversal patterns to generate a composite exit score (0-100).
- **`risk_manager.py` & `signal_logger.py`**: Handle position sizing (based on ATR/SL risk), daily max loss limits, and local CSV logging.

---

## 2. Recent Major Implementations

### A. The Position Exit Guardian (`position_exit_guardian.py`)
Historically, exits were coupled to the 3-minute entry scan, leading to delayed exits. We built a standalone daemon with the following logic:
- **Fast Polling**: Checks open positions every 30 seconds.
- **Dual-Timeframe Confirmation**: Evaluates reversal signals on both 3-minute and 5-minute timeframes. Strong exits (like immediate market close) require agreement across both timeframes or an extremely high score on one, filtering out single-timeframe noise.
- **Graduated Response Tiers**:
  - `HOLD` (Score < 40): Do nothing.
  - `TIGHTEN` (Score 40-59): Modifies trailing stop loss to tightly hug price (1x ATR).
  - `PARTIAL EXIT` (Score 60-74 in profit): Built `reduce_position()` in `dhan_integration.py`. *(Note: Currently falls back to TIGHTEN for Super Orders to prevent breaking bracket logic).*
  - `FULL EXIT` (Score ≥ 75, or PnL < -2.0%): Triggers an immediate exit.
- **Confirmation Cooldown**: When a Full Exit signal triggers, it is "armed" and waits exactly one extra cycle (30 seconds) to re-verify. If the reversal signal persists, it exits. This prevents whipsawing on a single volatile wick.
- **Dynamic Thresholds**: 
  - Exits become 10 points more aggressive after 3:00 PM IST.
  - Exits become more aggressive if the position is actively losing >1.5%.
- **Detailed Telegram Alerts**: Now extracts the specific reversal reason (e.g., `REV-VOLUME-CLIMAX`, `REV-RSI-DIVERGENCE`) and sends it as the exact tag to Telegram via `_exit_position`.

### B. Clean Super Order Management
- **Issue fixed**: Exiting a position generated by a Dhan Super Order (Bracket/Cover order) by simply placing an opposite INTRA market order would leave the target and stop-loss legs hanging open.
- **Solution**: Updated `_exit_position` in `stock_trading_bot.py`. It now explicitly checks if the order was a Super Order. If so, it calls `dhan.cancel_super_order(order_id, "TARGET_LEG")` and `"STOP_LOSS_LEG"` to safely dismantle the bracket *before* placing the market exit order.

### C. Intelligent Pre-Filters (`_passes_prefilter`)
To drastically reduce LLM API costs and filter out market noise, hard mathematical gates were added before sending data to the AI:
- **Choppy vs. Trending Mode**:
  - If Price > VWAP and Price > SMA20, it is in **Trending Mode**. RSI dead-zones are bypassed, allowing entries on smooth trends.
  - Otherwise, it is in **Choppy Mode**. Strict RSI boundaries (reject 42-58 without high volume) and higher ATR floors apply.
- **Universal Gates**:
  - ADX must be ≥ 18 (filters out completely dead/ranging markets).
  - Volume ratio must be > 0.15x average.

### D. Multi-Timeframe (MTF) Veto
After the AI generates a signal, `_validate_mtf_alignment` ensures we don't trade against the macro trend. 
- A BUY is vetoed if both the 15-minute and 1-hour SMA trends are BEARISH.
- A SELL is vetoed if both 15m and 1h are BULLISH.

### E. Kronos Bot Upgrade & Self-Improving Reflection (v2)
- **Model Size Upgrade**: Upgraded the Kronos bot from the 24.7M parameter model `NeoQuasar/Kronos-small` to the larger 102.3M parameter model `NeoQuasar/Kronos-base` to capture non-linear market regimes.
- **HDFCBANK Simulation Results**: Evaluated the upgraded `Kronos-base` on 5-minute bars of HDFCBANK (over 750 bars):
  - **MAE**: Rs 4.10 (0.53% average error)
  - **RMSE**: Rs 6.46
  - **MAPE**: 0.53%
  - **Horizon Decay**: MAE ranges from Rs 2.43 at t+1 up to Rs 5.85 at t+20.
- **Generalized Reflection Agent**: Refactored `reflect.py` and `run_weekly_reflection.py` to support the Kronos strategy. It now supports:
  - CLI options: `--strategy-file`, `--goal-file`, `--state-dir`, and `--kronos`.
  - Nested dictionary strategy configurations (`params` key nesting).
  - Version increments utilizing custom prefix recognition (`kronos-v01` -> `kronos-v02`).
  - Optimization logs and hypotheses isolated in `kronos_integrated_bot/state/`.
  - Tuning of all Kronos-specific parameters (`kronos_confidence_weight`, `kronos_penalty_conflict`, `kronos_bonus_align`, etc.).

### F. Complete Sector Mapping & Real-Time Regime Alignment
To improve regime detection accuracy and filter out low-probability trades in index-counter trends:
- **Comprehensive Sector Mapping**: Expanded `STOCK_SECTOR_MAP` in `regime_filter.py` from ~34 to 134 entries, covering the entire active watchlist.
- **Real-Time Index Quotes**: Replaced daily closes with real-time LTP from Dhan's `IDX_I` API segment via `_fetch_live_index_prices()`.
- **Sector Trend Conflict Penalty**: Implements a confidence penalty of `-12` points (`SECTOR_REGIME_PENALTY = 12`) if a signal opposes the sectoral trend.
- **Improved Logging**: Appends sector penalty details to the reasoning log.

### G. Pre-Computed Confidence Scoring Matrix & Programmatic Capping
To address the AI's chronic optimism bias, implemented an authoritative confidence scoring matrix in `enhanced_bot.py`:
- **Pre-computed Scoring Ceiling**: Raw technical indicators, MTF trend alignments, Nifty/Sector trends, and Kronos model outputs are fed into `_compute_score_matrix` to generate an algorithmic confidence ceiling (0-95).
- **Rule-based Penalties**: Subtracts points for weak volume, Kronos conflicts, MTF disagreement, overbought/oversold MFI, Nifty/Sector opposition, and low ADX.
- **Programmatic Capping**: `confidence = min(confidence, matrix_score)`.

### H. Caveman-Style Honesty Prompts
Redesigned prompt injections in `deepseek_analyzer.py` using blunt language to force honest confidence scores.

### I. Overextension Protection: VWAP Distance Cap
`MAX_VWAP_DISTANCE_PCT = 1.0` — blocks trades where price is >1% away from VWAP.

### J. Strict 15m Trend Alignment
Switched 15-minute trend to EMA-9 with no neutral state — must be strictly BULLISH for BUY, BEARISH for SELL.

### K. Entry Lag Reduction Rules
ADX bypass when volume_ratio >= 1.8. EMA-9 crossover triggers trending state alongside SMA-20.

### L. Guardian Startup Race Condition Fix (2026-06-04)
Added 15-second startup delay in `enhanced_guardian.py`. Increased retry backoff in `dhan_integration.py`.

### M. DeepSeek JSON Truncation Repair (2026-06-04)
Increased `max_tokens` 800→1500. Added `_repair_truncated_json()` fallback.

### N. Signal Logging & Telegram for All Signals Including Failed Orders (2026-06-04)
Restructured order flow — removed early return on failure, uses `order_failed` flag.

### O. Kronos Reflection & Low-Volatility Filter (2026-06-04)
Increased `MIN_PREFILTER_ATR_PCT` from 0.10 to 0.30 based on reflection agent analysis of 59 trades.

### P. Concurrency & Rate Limit Architecture Overhaul (2026-06-04)
Dual semaphores (`_dhan_sem=8`, `_ai_sem=3`). Asyncio-to-thread wrapping. Parallel guardian fetches. Thread-safe CSV logging.

### Q. Token State Overrides & DH-906 / DH-904 Mitigation (2026-06-05)
`load_dotenv(override=True)` in all entry points. Retry logic for DH-901/904.

### R. Pre-AI Quality Gate — RSI Extremes & Cross-TF Ranging (2026-06-06)
Pre-AI gates: RSI extremes (OB=78, OS=22) and cross-TF ADX check. Saves ~40-60% API tokens.

### S. Four New Signal Quality Filters (2026-06-06)
1. Stock-specific ATR percentile filter (daily 20th percentile)
2. Reversal detection on entry (score >= 40 veto)
3. Volume exhaustion check
4. Forced env overrides + concurrency reduction (`_dhan_sem=2`)

### T. Thread-Safety & Race Condition Fixes in Kronos Model (2026-06-08)
Stateless RoPE in `model/module.py` for thread-safe concurrent inference.

### U. DeepSeek JSON Parsing Fix & Fallback Enhancement (2026-06-09)
Strict single-quote enforcement in prompts. Robust reasoning recovery in `_repair_truncated_json`.

### V. Cross-Timeframe ATR Fix & Pipeline Optimizations (2026-06-09)
1. ATR percentile bug: compared daily ATR% vs daily floor (not 3-min)
2. MTF indicator min bars: 15m=25, 1h=15
3. Matrix gate before DeepSeek: skip if matrix < MIN_CONFIDENCE
4. START score raised 95→100
5. Position sizing formula fixed
6. Peer alignment filter removed
7. Reflection v13: min_volume_ratio_trending 0.4→0.3

### W. Intraday-Specific Parameters & Reflection Agent Expansion (2026-06-09)
The reflection agent had been cycling through only 3 parameters (confidence, ATR, volume) across 13 versions. Added 11 new intraday-focused parameters to give the LLM more degrees of freedom:

**Trailing & Exit Management:**
- `trailing_sl_activation_pct` (1.0-8.0, default 3.0): Profit % at which trailing SL activates. Below this, trailing_sl=0 (inactive). After activation, SL trails at `trailing_sl_distance_atr * ATR` behind price.
- `trailing_sl_distance_atr` (1.0-4.0, default 2.0): Distance of trailing SL in ATR multiples.
- `max_trade_duration_minutes` (30-240, default 180): Time-based forced exit via `TIME-EXIT`.
- `market_close_exit_minutes` (5-30, default 15): Exit all positions N minutes before 3:30 PM close.

**Opening & Risk Circuit Breakers:**
- `market_open_skip_minutes` (0-30, default 0): Skip entries for N minutes after 9:15 AM open.
- `max_consecutive_losses` (1-5, default 3): Stops ALL new entries after N consecutive losing trades.
- `partial_profit_pct` (0.0-50.0, default 0): EXPERIMENTAL — % of position to book at first target.
- `max_daily_loss_pct` (1.0-5.0, default 2.0): Max % portfolio loss before stopping for the day.

**Position Sizing:**
- `position_confidence_scalar` (0.5-2.0, default 1.0): Position size multiplier when AI confidence >= 85.
- `max_daily_trades` (2-10, default 5): Hard cap on total trades/day.
- `risk_per_trade_pct` (0.5-3.0, default 2.0): % of capital risked per trade.

**Implementation:**
- `enhanced_guardian.py`: New `_intraday_exit()` for time/close exits. Trailing SL activation logic. Consecutive loss tracking.
- `position_exit_guardian.py`: Same trailing activation + time exit for base bot.
- `enhanced_bot.py`: Open/close skip checks. Confidence scalar. Configurable trail distance.
- `reflect.py`: Param guide, `allowed_params`, `param_choices`, `all_params` all updated with 11 new entries. Version `kronos-v14`.

### X. Repository Cleanup & Kronos Integration Consolidation (2026-06-09)
- **Primary Entry Point**: The enhanced bot (`run_enhanced.py`) was moved and is now launched via `python -m kronos_integrated_bot.main`.
- **Kronos Architecture**: The new entry point loads `kronos_strategy.yaml` (v14) and runs `EnhancedIntradayBot` along with the Kronos time-series overlay, confidence scoring matrix, and the `KronosExitGuardian`. This includes all new intraday parameters (trailing SL activation, time-based exits, consecutive loss breaker).
- **Legacy Cleanup**: Removed unused legacy components (including `streamlit_app.py`, old `main.py`, `run_enhanced.py`, and the legacy `kronos_trading/` directory) to consolidate around the new integrated architecture. `trading_logs/` was retained.

## 3. Reversal Detection Logic (v3)
The `reversal_detector.py` uses a weighted scoring system (0-100).
- **Primary Signals (+12 points each when stacked)**: RSI Divergence, Volume Climax, VWAP Breach, MACD Crossovers.
- **Secondary Signals (+4 points each when stacked)**: Bollinger Breakout/Breakdown, 3-Candle Reversal, EMA/SMA Crossovers, MFI Extremes.
- Single signals are heavily penalized (discounted to 45% or 70% of base severity).

---

## 4. Known Behaviors & Edge Cases to Remember

- **Super Order Limitations**: The Dhan SDK has strict price validation for market orders inside Super Orders. We bypassed this by modifying `payload["price"] = None` in `dhan_integration.py -> place_super_order`.
- **Dry-Run Mode**: Both bots support a shared simulated `active_trades` dictionary. If `DRY_RUN = True`, API orders are bypassed, but trailing stops and Guardian logic execute normally.
- **Cooldowns**: 
  - `stock_trading_bot.py` has a 15-minute (`900s`) cooldown on entry signals for the same symbol to prevent duplicate spam. 
  - Even if a signal is generated but rejected due to insufficient capital (`quantity=0`), the cooldown is applied.
- **ATR Profiles**: Pre-warmed on first `_analyze` call for ALL watchlist stocks via background tasks (throttled by `_dhan_sem`). First scan cycle for each stock passes (profile still building). By the second scan cycle (~3 min later for each stock), profiles are ready. Profiles persist for the bot's lifetime (reset on restart). Compares daily ATR% vs daily 20th percentile floor — **not** 3-min ATR%.
- **Position Sizing Caveat**: With small capital (e.g., ₹2231), expensive stocks (₹5000+) correctly return qty=0 and are skipped. The bot only trades stocks it can afford at least 1 share of.
- **DH-901 vs DH-904**: Dhan sometimes returns `DH-901 Invalid_Authentication` when the real issue is rate limiting (`DH-904`). The guardian and fetch_positions retry logic accounts for this with longer backoffs.
- **Matrix Gate**: Enhanced bot skips DeepSeek if pre-computed scoring matrix ceiling < MIN_CONFIDENCE. Saves tokens on mathematically impossible setups.
- **DH-905 Invalid IP**: Dhan requires whitelisting your machine's public IP in the developer portal. If your ISP rotates IPs, orders will fail with this error. Set IP to `0.0.0.0` on the portal to allow all IPs if this recurs.
- **DeepSeek Token Limits**: The penalty-matrix reasoning can produce verbose responses. If truncation still occurs at 1500 tokens, the `_repair_truncated_json()` fallback extracts signal/confidence via regex.
- **MTF Bar Minimums**: `MIN_BARS_15M=25`, `MIN_BARS_1H=15` — ensures sufficient data for TALIB indicators (SMA-20, RSI-14, ADX-14, ATR-14). Below these, timeframe indicators are skipped (empty dict) rather than returning garbage fallback values.

## 5. Future Development Ideas
1. **Refine Partial Exits**: Implement logic to `modify_super_order` (reduce quantity of entry/target legs) instead of just canceling them, so we can safely partial-exit Super Orders.
2. **Options Integration**: Expand the bot to read Nifty/BankNifty option chains and trade liquid ATM contracts based on the index's AI signal.
